NewsRadar Frontend
==================

This is a simple React frontend that calls the backend at http://localhost:4000/api/news

Run:
  cd frontend
  npm install
  npm start

Change API base in src/utils/api.js if needed.
